import { ITrade } from '../db/trade-entity';
import { addVwap } from '../db/vwap.entity';
import { log } from './logger';

// export const getVwap = (values: IPriceVolumeData[]): number => {
//   return vwap([values]);
// };
